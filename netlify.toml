[build]
  base    = "app"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Redirect root to app (if ever needed)
[[redirects]]
  from   = "/*"
  to     = "/:splat"
  status = 200
